using System;

public class program
{
	public static void Main() 
	{
	 int numero;
		Console.WriteLine("Ingrese un número"); 
		numero = Int32.Parse(Console.ReadLine());
	
		Console.WriteLine(string.Concat(numero, " ",numero," ",numero," ", numero));
      
		Console.WriteLine(string.Concat(numero,numero,numero,numero));
		Console.WriteLine(string.Concat(numero, " ",numero," ",numero," ", numero));
      
		Console.WriteLine(string.Concat(numero,numero,numero,numero));
      Console.ReadLine();
        }
}
